---
allTasks:
  piotrek:
    tasks:
    - description: "description one"
      project: "test project"
      schedule:
    - description: "description two"
      project: "test project"
      schedule:
        cron: "*/5 1-3 * * *"
      publishers:
      - todoist
users:
  piotrek:
    todoist:
      enabled: true
      apiToken: "123456"
  zenobiusz:
  frydwulfa:
    todoist:
      enabled: false